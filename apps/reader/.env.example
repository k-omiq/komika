# Unified backend GraphQL endpoint (Suwayomi/Tachidesk + Komika services)
PUBLIC_KOMIKA_API=http://localhost:4567/api/graphql

# Cloudflare Worker image proxy base URL (web build only; native fetches direct)
PUBLIC_KOMIKA_IMG_WORKER=http://localhost:8787
